// Generated from up/tornado/app/onboard/brake_manager/BUILD — DO NOT EDIT BY HAND.
// Translates the vehicle_os_signal_junction into an Artheia node.
// Synthetic TIPC address allocated by fusée; the real runtime uses DDS topics.

package vendor.tornado.system.components.brake_manager

import vendor.tornado.system.signals.adas.*
import vendor.tornado.system.signals.chassis.*
import vendor.tornado.system.signals.powertrain.*
import vendor.tornado.system.signals.vehicle.*

// ---- forward-decl stubs for senderReceiver interfaces ----
// (real declarations live in vendor.tornado.system.signals.<cat>)
// origin: vendor.tornado.system.signals.adas
interface senderReceiver adas_brake_request { }
// origin: vendor.tornado.system.signals.chassis
interface senderReceiver brake_request { }
// origin: vendor.tornado.system.signals.chassis
interface senderReceiver brake_system_command { }
// origin: vendor.tornado.system.signals.chassis
interface senderReceiver gear_state { }
// origin: vendor.tornado.system.signals.powertrain
interface senderReceiver electric_motor_torque { }
// origin: vendor.tornado.system.signals.powertrain
interface senderReceiver torque_path_command { }
// origin: vendor.tornado.system.signals.vehicle
interface senderReceiver brake_intent { }
// origin: vendor.tornado.system.signals.vehicle
interface senderReceiver vehicle_speed { }

node atomic BrakeManager {
    tipc type=0x90000000 instance=0
    ports {
        receiver in_adas_brake_request requires adas_brake_request best_effort
        receiver in_gear_state requires gear_state best_effort
        receiver in_electric_motor_torque requires electric_motor_torque best_effort
        receiver in_torque_path_command requires torque_path_command best_effort
        receiver in_brake_intent requires brake_intent best_effort
        receiver in_vehicle_speed requires vehicle_speed best_effort
        sender   out_brake_request provides brake_request best_effort
        sender   out_brake_system_command provides brake_system_command best_effort
    }
}
