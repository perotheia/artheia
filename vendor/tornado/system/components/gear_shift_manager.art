// Generated from up/tornado/app/onboard/gear_shift_manager/BUILD — DO NOT EDIT BY HAND.
// Translates the vehicle_os_signal_junction into an Artheia node.
// Synthetic TIPC address allocated by fusée; the real runtime uses DDS topics.

package vendor.tornado.system.components.gear_shift_manager

import vendor.tornado.system.signals.body.*
import vendor.tornado.system.signals.chassis.*
import vendor.tornado.system.signals.common_types.*
import vendor.tornado.system.signals.vehicle.*

// ---- forward-decl stubs for senderReceiver interfaces ----
// (real declarations live in vendor.tornado.system.signals.<cat>)
// origin: vendor.tornado.system.signals.body
interface senderReceiver front_left_door_open_close { }
// origin: vendor.tornado.system.signals.chassis
interface senderReceiver gear_intent { }
// origin: vendor.tornado.system.signals.chassis
interface senderReceiver gear_intent_response { }
// origin: vendor.tornado.system.signals.chassis
interface senderReceiver gear_state { }
// origin: vendor.tornado.system.signals.common_types
interface senderReceiver vehicle_state { }
// origin: vendor.tornado.system.signals.vehicle
interface senderReceiver brake_intent { }
// origin: vendor.tornado.system.signals.vehicle
interface senderReceiver vehicle_power_state { }
// origin: vendor.tornado.system.signals.vehicle
interface senderReceiver vehicle_speed { }

node atomic GearShiftManager {
    tipc type=0x90000003 instance=0
    ports {
        receiver in_front_left_door_open_close requires front_left_door_open_close best_effort
        receiver in_gear_intent requires gear_intent best_effort
        receiver in_vehicle_state requires vehicle_state best_effort
        receiver in_brake_intent requires brake_intent best_effort
        receiver in_vehicle_power_state requires vehicle_power_state best_effort
        receiver in_vehicle_speed requires vehicle_speed best_effort
        sender   out_gear_intent_response provides gear_intent_response best_effort
        sender   out_gear_state provides gear_state best_effort
    }
}
