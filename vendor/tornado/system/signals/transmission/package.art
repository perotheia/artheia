// Generated from up/tornado/signals/transmission/ — DO NOT EDIT BY HAND.
// Lossy proto2 → Artheia transcription. See artheia/importers/fusee.py.
// Each `vehicle_os_signal(...)` in the original BUILD is a DDS topic
// (many-to-many pub/sub). Captured here as a senderReceiver interface;
// the system.art composition wires every (publisher, subscriber) pair.

package vendor.tornado.system.signals.transmission

// ---- enums (flattened to uint32 at use sites) ----
// enum DriveModeState: DRIVE_MODE_STATE_NORMAL=0, DRIVE_MODE_STATE_SPORT=1, DRIVE_MODE_STATE_RACETRACK=2, DRIVE_MODE_STATE_ECO=3

message DriveMode {
    uint32 drive_mode = 1  // required, originally enum DriveModeState
}

message DriveModeRequest {
    bool get_current_drive_mode = 1  // required
}

// ---- DDS topics (vehicle_os_signal in original BUILD) ----
interface senderReceiver drive_mode {
    data DriveMode payload
}

interface senderReceiver drive_mode_to_light_manager {
    data DriveMode payload
}

interface senderReceiver drive_mode_to_app {
    data DriveMode payload
}

interface senderReceiver drive_mode_request {
    data DriveModeRequest payload
}
