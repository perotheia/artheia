__pycache__/
*.py[cod]
*.egg-info/
.venv/
build/
dist/
.pytest_cache/
generated/

# Vendored upstream source — not ours to commit. autosar-data is pinned
# in pyproject.toml [importers] and installed from PyPI.
up/

# Generated by fusée from up/tornado/. Regenerate with
# `python -m artheia.importers.fusee` (or via the importer module). Not
# ours to commit; treat as build output.
vendor/tornado/
