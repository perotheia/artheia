# AUTO-GENERATED by `artheia gen-app --kind fc` — DO NOT EDIT.
# source: {{ source_file }}
#
# Lib slice — the daemon class declaration + message wiring. The
# impl slice's BUILD.bazel pulls this in for the cc_binary; the
# top-level BUILD.bazel may also expose this for downstream
# integration tests.

load("@rules_cc//cc:defs.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "{{ model.fc_short }}_lib",
    hdrs = [
{%- for n in model.nodes %}
        "{{ n.name }}.hh",
        "{{ n.name }}_netgraph.hh",
{%- endfor %}
        "{{ model.fc_short }}_codecs.hh",
        "Log.hh",
{%- if model.nodes | selectattr("requires_rds") | list %}
        "RdsStreams.hh",
{%- endif %}
    ],
    includes = [".."],
    copts = [
        "-std=c++17",
        "-Wall",
        "-Wextra",
        "-fPIC",
    ],
    deps = [
        "//platform/runtime:runtime",
        "{{ proto_label }}",
        # The State structs the lib binds as the GenServer State type live
        # in the (write-once, app-owned) impl/<Node>_state.hh headers,
        # exposed by the leaf {{ model.fc_short }}_state target — depended
        # on here so the lib<-impl include is a clean DAG, not a cycle.
        "//{{ model.bazel_pkg_prefix }}/impl:{{ model.fc_short }}_state",
{%- if model.nodes | selectattr("requires_rds") | list %}
        # RDS handles (RdsStreams.hh → ara::rds::StreamWriter/Reader).
        "//services/rds/transport:rds",
{%- endif %}
    ],
)
