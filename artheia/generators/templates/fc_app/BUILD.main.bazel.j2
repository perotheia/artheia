# AUTO-GENERATED by `artheia gen-app --kind fc` — DO NOT EDIT.
# source: {{ source_file }}
#
# Main slice — the cc_binary entry point. Pulls in lib (class
# decl + message wiring) + impl (handler bodies).

load("@rules_cc//cc:defs.bzl", "cc_binary")

package(default_visibility = ["//visibility:public"])

cc_binary(
    name = "{{ model.fc_short }}",
    srcs = ["main.cc"],
    copts = [
        "-std=c++17",
        "-Wall",
        "-Wextra",
    ],
    deps = [
        "//{{ model.bazel_pkg_prefix }}/lib:{{ model.fc_short }}_lib",
        "//{{ model.bazel_pkg_prefix }}/impl:{{ model.fc_short }}_impl",
        # Crash forensics — main.cc installs the fatal-signal handler.
        "{{ platform_repo }}//platform/supervisor/tombstone:tombstone",
{%- if model.nodes | selectattr("requires_rds") | list %}
        # RDS zero-copy data plane (a requires_rds node) — ara::rds + iceoryx.
        "{{ platform_repo }}//services/rds/transport:rds",
{%- endif %}
    ],
    linkopts = [
        "-lpthread",
        "-l:libprotobuf-nanopb.a",
    ],
)
