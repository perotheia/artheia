# AUTO-GENERATED by `artheia gen-app --kind fc` — DO NOT EDIT.
# source: {{ source_file }}
#
# Main slice — the cc_binary entry point. Pulls in lib (class
# decl + message wiring) + impl (handler bodies).

load("@rules_cc//cc:defs.bzl", "cc_binary")

package(default_visibility = ["//visibility:public"])

cc_binary(
    name = "{{ model.fc_short }}",
    srcs = ["main.cc"],
    copts = [
        "-std=c++17",
        "-Wall",
        "-Wextra",
    ],
    deps = [
        "//services/{{ model.fc_short }}/lib:{{ model.fc_short }}_lib",
        "//services/{{ model.fc_short }}/impl:{{ model.fc_short }}_impl",
    ],
    linkopts = [
        "-lpthread",
        "-l:libprotobuf-nanopb.a",
    ],
)
