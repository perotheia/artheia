"""AUTO-GENERATED by `artheia gen-app --kind fc` — DO NOT EDIT.

source: {{ source_file }}

SwComponent + Executable for the {{ model.fc_short }} FC. Regenerated on
every gen-app run; rig overlays patch by name via the existing
Override mechanism in artheia.manifest.

The supervision strategy (RestartType, shutdown_ms, start_cmd) lives
next door in ``executor.py`` so a regen of this file doesn't squash
the rig integrator's hand-drafted supervision choices.
"""
from artheia.manifest.application import (
    BuildTypeEnum,
    Executable,
    ExecutionStateReportingBehaviorEnum,
    RootSwComponentPrototype,
    SwComponent,
)


COMPONENT = SwComponent(
    name="{{ model.fc_short }}",
    bazel_target="//services/system/{{ model.fc_short }}:{{ model.fc_short }}",
    owner="platform",
    art_node="{{ model.art_package }}/{{ model.daemon_class }}",
)


EXECUTABLE = Executable(
    name="{{ model.fc_short }}",
    category="PLATFORM_LEVEL",
    build_type=BuildTypeEnum.BUILD_TYPE_RELEASE,
    reporting_behavior=(
        ExecutionStateReportingBehaviorEnum.REPORTING_BEHAVIOR_INDIVIDUAL
    ),
    root_sw_component_prototype=RootSwComponentPrototype(
        name="{{ model.fc_short }}_root",
        application_type="{{ model.daemon_class }}",
    ),
)
