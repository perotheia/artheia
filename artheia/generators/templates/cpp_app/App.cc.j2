// AUTO-GENERATED by `artheia gen-app` — DO NOT EDIT.
// source: {{ source_file }}
// node:   {{ node.name }}
//
// Slice 2 (stub): runtime wiring. The lifecycle methods hold the
// integration point with the signal junction generated by libgw /
// codegen; user callback bodies live in impl/{{ node.name }}_handlers.cc.
//
// Today the signal-junction binding is a TODO — it lands when the
// host-side proto + dispatch generation does. The OnCreate body
// below is a placeholder that compiles cleanly against the forward-
// declared {{ node.snake }}_signals struct.

#include "{{ node.name }}.hh"

namespace {{ namespace }} {

{{ node.name }}::{{ node.name }}(const {{ node.name }}Inputs& inputs)
    : inputs_(inputs) {}

void {{ node.name }}::OnCreate() noexcept {
  // TODO(codegen): wire signal subscriptions. Per receiver port:
  //   inputs_.signals_.subscribe_<port>(
  //       [this](const std::shared_ptr<{{ "{{Msg}}" }}> msg) noexcept {
  //         this->on_<port>(msg);
  //       });
  // The subscribe_<port> methods are emitted by the upcoming host-proto
  // generator (platform/runtime + libgw integration).
}

void {{ node.name }}::OnStart() noexcept {
  // Subscriptions become live here in the NIF reactor; override the
  // body in impl/ if you need periodic timers or initial-state reads.
}

void {{ node.name }}::OnStop() noexcept {
  // Cleanup hook. The signal junction tears itself down on destruction.
}

{% for port in node.client_ports -%}
int {{ node.name }}::{{ port.call }}() noexcept {
  // TODO(codegen): forward to the signal-junction RPC stub.
  //   return inputs_.signals_.{{ port.call_dispatch }}();
  return -1;
}
{% endfor %}

}  // namespace {{ namespace }}
